import type { Product } from '../types/product'
import { useLanguage } from '../i18n'
export function ConfirmModal({ product, busy, onConfirm, onCancel }: { product: Product; busy: boolean; onConfirm: () => void; onCancel: () => void }) { const { t } = useLanguage(); return <div className="fixed inset-0 z-50 grid place-items-end bg-slate-950/45 p-4 backdrop-blur-sm sm:place-items-center" role="dialog" aria-modal="true" aria-labelledby="delete-title"><div className="w-full max-w-md rounded-3xl bg-white p-6 shadow-2xl"><div className="grid h-12 w-12 place-items-center rounded-2xl bg-rose-50 font-black text-rose-600">!</div><h2 id="delete-title" className="mt-5 text-xl font-bold">{t.confirmDelete}: {product.itemno}?</h2><p className="mt-2 text-sm leading-6 text-slate-500">{t.deleteCopy}</p><div className="mt-7 grid grid-cols-2 gap-3"><button type="button" disabled={busy} onClick={onCancel} className="btn-secondary">{t.cancel}</button><button type="button" disabled={busy} onClick={onConfirm} className="rounded-xl bg-rose-600 px-4 py-3 text-sm font-bold text-white disabled:opacity-50">{busy ? t.deleting : t.confirmDelete}</button></div></div></div> }
