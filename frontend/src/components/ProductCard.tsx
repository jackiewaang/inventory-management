import { useState } from 'react'
import { productApi } from '../api/products'
import { useLanguage } from '../i18n'
import type { Product } from '../types/product'
export interface ProductCardProps { product:Product;onOpen:(product:Product)=>void }
export function ProductCard({product,onOpen}:ProductCardProps){const[show,setShow]=useState(false);const{language,t}=useLanguage();const money=(v:string)=>new Intl.NumberFormat(language==='zh'?'zh-CN':'en-IE',{style:'currency',currency:'EUR'}).format(Number(v));const image=productApi.imageUrl(product.image_path);const tags=product.tags?.split(',').map(x=>x.trim()).filter(Boolean)??[];return <article className="overflow-hidden rounded-3xl border border-slate-200 bg-white shadow-sm transition hover:shadow-lg"><button type="button" onClick={()=>onOpen(product)} className="block w-full text-left"><div className="aspect-[4/3] bg-slate-100">{image?<img src={image} alt={`${t.item} ${product.itemno}`} className="h-full w-full object-cover"/>:<div className="grid h-full place-items-center text-sm text-slate-400">{t.noImage}</div>}</div><div className="p-5"><div className="flex justify-between gap-3"><div><p className="text-xs font-bold uppercase tracking-widest text-slate-400">{t.item}</p><h2 className="mt-1 text-lg font-bold">{product.itemno}</h2></div><div className="text-right"><p className="text-xs text-slate-400">{t.selling}</p><p className="mt-1 font-bold text-teal-700">{money(product.selling_price)}</p></div></div>{tags.length>0&&<div className="mt-4 flex flex-wrap gap-1.5">{tags.map(tag=><span key={tag} className="rounded-full bg-slate-100 px-2.5 py-1 text-xs font-semibold text-slate-600">{tag}</span>)}</div>}</div></button><div className="mx-5 mb-5 flex items-center justify-between rounded-xl bg-slate-50 px-3 py-2.5"><span className="text-xs font-semibold text-slate-500">{t.buying}</span><button type="button" onClick={()=>setShow(v=>!v)} className="rounded-lg px-2 py-1 text-xs font-bold text-slate-700">{show?money(product.buying_price):t.reveal}</button></div></article>}
